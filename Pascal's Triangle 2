def getRow(self, rowIndex):
        if rowIndex == 0: return [1]
        if rowIndex == 1: return [1]*2
        else:
            prevRow = self.getRow(rowIndex-1)
            row = [1]*(rowIndex+1)
            for i in range(1, len(prevRow)): row[i] = prevRow[i-1]+prevRow[i]
        return row
