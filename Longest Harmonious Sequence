def findLHS(self, nums):
        numbers = {}
        total = 0
        for i in nums: numbers[i] = 1+numbers.get(i,0)   
        if(len(numbers) == 1): return 0
        for i in numbers: 
            if(numbers.get(i+1,0) != 0): total = max(total, numbers[i]+numbers[i+1])
        return total
